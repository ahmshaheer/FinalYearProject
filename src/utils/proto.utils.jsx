console.log('Protobuf utils')

const encodeProtobuf = (boundingBoxes) => {
  console.log('encode')
}

const decodeProtobuf = (boundingBoxes) => {
  console.log('decode')
}

export { encodeProtobuf, decodeProtobuf }
